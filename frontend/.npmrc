# npm configuration for consistent behavior across machines

# Use exact versions from package-lock.json
engine-strict=false

# Set registry to default npm registry
registry=https://registry.npmjs.org/

# Disable audit if needed (can be enabled for security checks)
# audit=false

# Cache management
prefer-offline=true

# Use exact versions
save-exact=false
